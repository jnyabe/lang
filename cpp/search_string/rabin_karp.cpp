#include "rabin_karp.h"

int RabinKarp::Search(const std::string& text, const std::string &pattern)
{
	int idx = -1;
	return idx;
}


int RabinKarp::Search(const std::string& text
				  , const std::vector<std::string> &pattern)
{
	int idx = -1;
	return idx;	
}
